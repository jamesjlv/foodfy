{% extends "../layout.njk" %}

{% block content %}

  <div class="content_admin_chefs">
    <h1>Gerenciar Chefs</h1>

    <div class="chefs">
      {% for chef in chefs %}
        <div class="item">
          <div class="avatar"><img src="{{chef.avatar_url}}" alt="Foto do chef"></div>
          <div class="details">
            <h2>{{chef.name}}</h2>
            <a href="/admin/chefs/{{chef.id}}">Visualizar</a>
          </div>
        </div>
      {% endfor %}
    </div>

  </div>

{% endblock content %}