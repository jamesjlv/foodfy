{%extends "admin/layout.njk"%}

{% block content %}
  <div class="content_index_admin">
    <h1>Gerenciar receitas</h1>
    <div class="card">
      {% for recipe in recipes %}
        <div class="item">
          <div class="img"><img src="{{recipe.image}}"></div>
          <div class="item_content">
            <h2>{{recipe.title}}</h2>
            <h3>{{recipe.author}}</h3>
            <a href="/admin/recipes/{{loop.index0}}">Visualizar</a>
          </div>
        </div>
      {% endfor %}
    </div>
  </div>
{% endblock content %}